/// Base URL for the FE for Raccoons backend (the same API the website uses).
///
/// Default points at production. For local backend work, run the service
/// (`service/`) and point this at `http://localhost:4000/api` — the iOS
/// simulator can reach the host's localhost directly.
const String apiBaseUrl = 'https://fe4raccoons.com/api';
